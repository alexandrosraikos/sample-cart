/**
 * The cart component.
 * 
 * @param {string} props.className The component class name.
 */
const Cart = ({ className }) => {
    return (
        <div className={`${className} my-4 p-6 bordered`}>
        </div>
    )
}

export default Cart